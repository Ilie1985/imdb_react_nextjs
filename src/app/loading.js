import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center">
      <p className="text-amber-600 text-lg font-semibold">Loading...</p>
    </div>
  );
};

export default Loading;
